# Side Glance project instructions

Before changing this repository, read `CLAUDE.md` completely. Its working
agreement, red-green TDD order, required verification gates, safety invariants,
and product-claim boundaries are mandatory when working in Codex.

Use Node 24.18.0 from `.nvmrc` and run commands from the repository root.
Keep the landing page and CLI aligned with the shared Side Glance lifecycle theme.
